import { koPageMetadata } from "@/lib/i18n/metadata";

export const metadata = koPageMetadata("/insert");

export default function InsertLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
